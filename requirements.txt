# Base PyPI dependencies. Install Paddle separately:
#   pip install -r requirements-gpu.txt   (NVIDIA GPU, recommended)
#   pip install -r requirements-cpu.txt   (no GPU / non-NVIDIA)
# Or just run setup.bat once and it picks the right one.

opencv-python
paddleocr
moviepy
numpy
pandas
librosa
pysrt
webvtt-py
tqdm
python-bidi==0.4.2
rapidfuzz>=3.0.0
